name: Build PWA (reusable)

on:
  workflow_call:
    inputs:
      node-version:
        type: string
        default: '22'
      build-command:
        type: string
        default: 'pnpm run build'
      artifact-name:
        type: string
        default: 'pwa-dist'
      tarball-name:
        type: string
        default: 'xiboplayer-pwa-dist.tar.gz'
      cache-deps:
        type: boolean
        default: true
        description: 'Cache pnpm dependencies (requires lockfile — set false if no lockfile committed)'
      install-command:
        type: string
        default: 'pnpm install --frozen-lockfile'
        description: 'Install command (use --no-frozen-lockfile for repos with workspace overrides)'
      release-name:
        type: string
        default: 'PWA Player'

jobs:
  build:
    name: Build PWA dist
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ inputs.node-version }}
          cache: ${{ inputs.cache-deps && 'pnpm' || '' }}

      - run: ${{ inputs.install-command }}

      - run: ${{ inputs.build-command }}

      - name: Create tarball
        run: tar -czf ${{ inputs.tarball-name }} -C dist .

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ inputs.artifact-name }}
          path: ${{ inputs.tarball-name }}
          retention-days: 30

  release:
    name: Create GitHub Release
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/download-artifact@v4
        with:
          name: ${{ inputs.artifact-name }}

      - uses: softprops/action-gh-release@v2
        with:
          name: "${{ inputs.release-name }} ${{ github.ref_name }}"
          files: ${{ inputs.tarball-name }}
          draft: false
          prerelease: false
